name: Keep BigRocks Alive

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:
*jobs:
  ping:
    runs-on: ubuntu-*atest

    steps:
      - name: Pi*g Streamlit
        run: |
       *  curl -L -s https://bigrocks.streamlit.app/ > /dev/null
